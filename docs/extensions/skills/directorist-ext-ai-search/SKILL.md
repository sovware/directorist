---
name: directorist-ext-ai-search
description: Investigate or extend Directorist AI Search in WordPress Directorist. Route ai search, semantic, indexing, api key, embedding, irrelevant results issues to source-backed feature docs and verify the installed implementation.
---

# Directorist AI Search

Read [identity and topic index](../../products/directorist-ai-search/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `main` at `07f49c7e6c2ba400e756f69b815b4febaf4d886f`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Index listing content for semantic discovery and integrate ranked results into Directorist searches.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
