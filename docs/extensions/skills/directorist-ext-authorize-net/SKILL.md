---
name: directorist-ext-authorize-net
description: Investigate or extend Authorize.net Payment Gateway in WordPress Directorist. Route authorize.net, authorize net, hosted payment, accept hosted, authorize webhook, authorize subscription issues to source-backed feature docs and verify the installed implementation.
---

# Authorize.net Payment Gateway

Read [identity and topic index](../../products/directorist-authorize-net/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `development` at `ab7986313aa349c4cea2d9956169ae2d07da4fd0`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Connect Authorize.net checkout and confirmation to Directorist payment flows.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
