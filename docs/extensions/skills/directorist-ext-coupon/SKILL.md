---
name: directorist-ext-coupon
description: Investigate or extend Directorist Coupon in WordPress Directorist. Route coupon, discount code, coupon expired, coupon limit, stripe discount, coupon renewal issues to source-backed feature docs and verify the installed implementation.
---

# Directorist Coupon

Read [identity and topic index](../../products/directorist-coupon/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `development` at `d98770d6b7dd98122f1be8fd0cd2038c25abe687`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Apply configured discounts to eligible checkout totals and track coupon usage.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
