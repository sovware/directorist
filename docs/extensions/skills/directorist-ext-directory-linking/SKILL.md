---
name: directorist-ext-directory-linking
description: Investigate or extend Multi Directory Linking in WordPress Directorist. Route directory linking, linked listing, multi directory linking, linked directory search, linked listing missing issues to source-backed feature docs and verify the installed implementation.
---

# Multi Directory Linking

Read [identity and topic index](../../products/directorist-directory-linking/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `alpha` at `ade10cf1e0303510a7c9ad18a925f0799932ce46`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Link a listing to other listings and expose linked-directory fields and search.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
