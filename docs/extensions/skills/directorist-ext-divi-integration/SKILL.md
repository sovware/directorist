---
name: directorist-ext-divi-integration
description: Investigate or extend Directorist Divi Integration in WordPress Directorist. Route divi search, divi listings, divi filter, divi pagination, divi, divi business hours issues to source-backed feature docs and verify the installed implementation.
---

# Directorist Divi Integration

Read [identity and topic index](../../products/directorist-divi-integration/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `master` at `e3c8cafacaeae1422a5fe56fba16a0eced9eee18`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Build native Divi directory layouts while preserving Directorist queries, fields and actions.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
