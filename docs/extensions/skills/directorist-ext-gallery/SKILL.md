---
name: directorist-ext-gallery
description: Investigate or extend Image Gallery in WordPress Directorist. Route image gallery, gallery upload, gallery saving, gallery cron, gallery import, gallery slider issues to source-backed feature docs and verify the installed implementation.
---

# Image Gallery

Read [identity and topic index](../../products/directorist-gallery/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `fix/gallery-import-cron-scheduling` at `34048acde9eba93c63d2eb7b66f5d5b1ce21ba3b`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Store listing image galleries, display them and import remote gallery attachments.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
