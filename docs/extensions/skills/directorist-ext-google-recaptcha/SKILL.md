---
name: directorist-ext-google-recaptcha
description: Investigate or extend Google reCAPTCHA in WordPress Directorist. Route recaptcha, captcha, spam protection, v3 score, captcha missing, captcha badge issues to source-backed feature docs and verify the installed implementation.
---

# Google reCAPTCHA

Read [identity and topic index](../../products/directorist-google-recaptcha/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `v3-support` at `ae5173102e9d6f4a69d52159d610bf45856de762`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Apply Google challenge/token validation to selected Directorist public forms.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
