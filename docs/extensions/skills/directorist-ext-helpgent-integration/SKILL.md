---
name: directorist-ext-helpgent-integration
description: Investigate or extend HelpGent Integration in WordPress Directorist. Route helpgent, video message, audio message, lead form, response access, helpgent contact issues to source-backed feature docs and verify the installed implementation.
---

# HelpGent Integration

Read [identity and topic index](../../products/directorist-helpgent-integration/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `master` at `681495a66d0f8072924937b8ae00e6f514ac68ab`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Expose scoped HelpGent forms, responses and contacts in Directorist user dashboards.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
