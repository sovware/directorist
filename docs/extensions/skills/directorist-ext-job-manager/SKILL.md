---
name: directorist-ext-job-manager
description: Investigate or extend Job Manager in WordPress Directorist. Route job manager, job salary, job deadline, open position, job application, resume upload issues to source-backed feature docs and verify the installed implementation.
---

# Job Manager

Read [identity and topic index](../../products/directorist-job-manager/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `development` at `562615e4ede94f2c41c79c02e15035f7b9e64a41`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Add job-specific fields and accept applications with attachments and email delivery.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
