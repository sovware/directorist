---
name: directorist-ext-listing-import
description: Investigate or extend Directorist Listing Importer in WordPress Directorist. Route google import, listing importer, place id, field mapping, import hours, rss import issues to source-backed feature docs and verify the installed implementation.
---

# Directorist Listing Importer

Read [identity and topic index](../../products/directorist-listing-import/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `main` at `81e621c5d45a938a1a21d9e0185f3f7528f9a660`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Import listings from Google Places and feeds with mapping, duplicate checks and scheduled runs.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
