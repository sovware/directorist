---
name: directorist-ext-listings-with-map
description: Investigate or extend Listings with Map in WordPress Directorist. Route listings with map, map pins, map sync, map pagination, map blank, map layout issues to source-backed feature docs and verify the installed implementation.
---

# Listings with Map

Read [identity and topic index](../../products/directorist-listings-with-map/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `codex/lwm-map-sync-extension` at `7a0d7a04f9b17b6053c8dee1f1440f84821268ad`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Render synchronized map/listing views with filtering and AJAX navigation.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
