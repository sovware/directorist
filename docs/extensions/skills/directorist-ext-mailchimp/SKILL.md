---
name: directorist-ext-mailchimp
description: Investigate or extend Mailchimp Integration in WordPress Directorist. Route mailchimp, newsletter signup, audience, mailchimp contact, contact newsletter issues to source-backed feature docs and verify the installed implementation.
---

# Mailchimp Integration

Read [identity and topic index](../../products/directorist-mailchimp/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `master` at `00943169669bf9caa01ae6ebb1c2a34fb62098ca`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Subscribe consenting directory users or contacts to a configured Mailchimp audience.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
