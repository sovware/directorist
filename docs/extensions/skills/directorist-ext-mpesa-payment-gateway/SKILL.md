---
name: directorist-ext-mpesa-payment-gateway
description: Investigate or extend M-Pesa Payment Gateway in WordPress Directorist. Route mpesa, m-pesa, stk push, kenyan phone, kes, ratiba issues to source-backed feature docs and verify the installed implementation.
---

# M-Pesa Payment Gateway

Read [identity and topic index](../../products/directorist-mpesa-payment-gateway/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `main` at `f304ecfa35049b5cc827b182f54dcbc048dc5bf3`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Collect M-Pesa payments with STK push and supported recurring schedules.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
