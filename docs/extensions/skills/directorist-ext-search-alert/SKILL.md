---
name: directorist-ext-search-alert
description: Investigate or extend Directorist Search Alert in WordPress Directorist. Route search alert, saved search, alert preferences, search alert email, daily alert, weekly alert issues to source-backed feature docs and verify the installed implementation.
---

# Directorist Search Alert

Read [identity and topic index](../../products/directorist-search-alert/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `main` at `4b62ac4632520a7e5ea2494ac30ca2b3756899df`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Save a user search and send matching-listing notifications on configured schedules.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
