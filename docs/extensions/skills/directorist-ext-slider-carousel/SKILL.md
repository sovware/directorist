---
name: directorist-ext-slider-carousel
description: Investigate or extend Listings Slider & Carousel in WordPress Directorist. Route listings slider, carousel, slider listings, slider order, carousel arrows, slider responsive issues to source-backed feature docs and verify the installed implementation.
---

# Listings Slider & Carousel

Read [identity and topic index](../../products/directorist-slider-carousel/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `beta` at `fa74bc9ece627c2c36bf5c2464dc2f3710fec438`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Display queried listings in a slider/carousel using extension shortcode settings.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
