---
name: directorist-ext-stripe
description: Investigate or extend Stripe Payment Gateway in WordPress Directorist. Route stripe, card payment, sepa, stripe tax, stripe coupon, stripe webhook issues to source-backed feature docs and verify the installed implementation.
---

# Stripe Payment Gateway

Read [identity and topic index](../../products/directorist-stripe/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `master` at `498c48755f4168fdfdcaa955fb817647287a95e1`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Collect card/Stripe payments and synchronize checkout, webhooks and subscriptions with Directorist orders.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
