---
name: directorist-ext-universal-search
description: Investigate or extend Directorist Universal Search in WordPress Directorist. Route universal search, all directories, cross directory search, universal form missing, universal results, search preview issues to source-backed feature docs and verify the installed implementation.
---

# Directorist Universal Search

Read [identity and topic index](../../products/directorist-universal-search/README.md), then only the issue-related topic linked there. A-Z is stored coverage, not a mandatory reading list. If the owner is uncertain, use [the small router](../../ROUTER.md); retain multiple candidates until evidence disambiguates them.

Canonical starting snapshot: `master` at `9a0d3ca53ba331cb1a473965b4c86d9904a6eb9a`. Match actual changed files, active plugin basename and source hashes before diagnosing; version/tag/default branch is not authoritative. Read source-variants only when the installed files differ.

Search across directory types using a dedicated search form and results route.

For ticket work follow [focused evidence workflow](../../WORKFLOW.md) and reuse `rabbi-support-delivery-orchestrator` only for the required generic stages. An unsuccessful local reproduction does not invalidate the issue. Source, local, client, CI and release proof remain separate. This skill grants no additional mutation, sending or publishing authorization.
