{
  "name": "sass-compile",
  "version": "1.0.0",
  "description": "A simple sass compile config.",
  "main": "index.js",
  "scripts": {
    "sass-dev": "sass --watch --update --style=expanded assets/scss/style.scss:assets/style.css",
    "sass-prod": "sass --no-source-map --style=compressed assets/scss:assets/css"
  },
  "author": "Adam",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.83.4"
  }
}
